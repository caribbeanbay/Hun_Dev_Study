# 4L 회고 - [문제명]

## 👍 Liked (좋았던 점)
- 단계별로 나눠서 풀이를 진행하여 복잡한 문제를 차근차근 접근할 수 있었다.
- 단어 파싱 결과를 print로 직접 확인하면서 진행하니 이해하기 쉬웠다.

## 📚 Learned (배운 것)
- 문자열을 직접 순회하며 단어의 시작/끝 인덱스를 추출하는 파싱 방법
- 두 구간의 겹침 조건: `ws <= re AND we >= rs`
- `any()`를 활용해 여러 구간 중 하나라도 겹치는지 간결하게 확인하는 방법
- `set`을 활용한 O(1)조회로 `blacklist`와 `seen`(중복 체크)구현

## 😅 Lacked (부족했던 점)
- 구현 문제임에도 어디서부터 시작해야 할지 막막했다.
- 겹침 조건을 스스로 도출하지 못하고 힌트가 필요했다.

## 🔥 Longed For (앞으로 할 것)
- 구현 문제를 풀 때 조건을 단게별로 나누는 습관 기르기
- 두 구간의 겹침/포함 조건을 자유자재로 쓸 수 있도록 익히기
- 같은 문제를 C++로도 풀어보기